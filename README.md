# DIS (Dual Inventory System)


### How to compile and run
1. cd build
2. cmake .. -G "Unix Makefiles"  ('cmake ..' on Unix)
3. make install
4. ..\bin\app.exe  ('../bin/app' on Unix)


### How to generate tags
In root directory:
- ctags -R --exclude=build
